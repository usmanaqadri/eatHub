<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background-color: black;
            color: white;
        }
        main {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>
<body>
    <header>
        <h1>EatHub</h1>
    </header>
    <main>
        <h2><%= item.name %></h2>
        <form method="POST" action="/eatHub?_method=PUT">
            <input name="<%= item.id %>" type="number" value="<%= item.qty %>" min="0" />
            <button>Update Quantity</button>
        </form>
    </main>
</body>
</html>